"This will serve as a template to start the code on bootup, but the final directory is subject to change"
"for the finalized flight code"

#!/bin/sh -e
#
# rc.local
#
# This script is executed at the end of each multiuser runlevel.
# Make sure that the script will "exit 0" on success or any other
# value on error.
#
# In order to enable or disable this script just change the execution
# bits.
#
# By default this script does nothing.

# Print the IP address
#python3 /home/pi/Downloads/Python_LJM_2018_10_19/Temp.Opto.Camera.Composite.py & > /dev/null 2>&1 &

exit 0
